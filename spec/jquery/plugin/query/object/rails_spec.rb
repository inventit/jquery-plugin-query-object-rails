RSpec.describe Jquery::Plugin::Query::Object::Rails do
  it "has a version number" do
    expect(Jquery::Plugin::Query::Object::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
